import "./style.scss";

const TodosHeader = () => {
  return (
    <section className="todosHeader">
      <h1>Todos List</h1>
      <h4>Get a clear overview of everything you need to do and never lose track of important tasks.</h4>
    </section>
  );
};

export default TodosHeader;
